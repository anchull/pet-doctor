<div class="h-screen bg-background text-white relative flex flex-col items-center justify-center p-6 overflow-hidden">
    <!-- Background Blurs -->
    <div class="absolute top-0 left-0 w-64 h-64 bg-primary/20 rounded-full blur-3xl -ml-16 -mt-16"></div>
    <div class="absolute bottom-0 right-0 w-64 h-64 bg-purple-500/10 rounded-full blur-3xl -mr-16 -mb-16"></div>

    <!-- Sliders Container -->
    <div id="slider-container" class="relative w-full max-w-md h-[400px]">
        <!-- Slide 1 -->
        <div id="slide-0"
            class="slide absolute inset-0 transition-opacity duration-500 opacity-100 flex flex-col items-center text-center">
            <div
                class="w-48 h-48 bg-white/5 rounded-full flex items-center justify-center mb-8 border border-white/10 shadow-xl shadow-primary/10">
                <svg class="w-24 h-24 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
                        d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
            </div>
            <h1 class="text-3xl font-bold mb-4">간편한 소변 검사</h1>
            <p class="text-gray-400">집에서 1분이면 끝!<br>동물병원에 가지 않고도 검사할 수 있어요.</p>
        </div>

        <!-- Slide 2 -->
        <div id="slide-1"
            class="slide absolute inset-0 transition-opacity duration-500 opacity-0 pointer-events-none flex flex-col items-center text-center">
            <div
                class="w-48 h-48 bg-white/5 rounded-full flex items-center justify-center mb-8 border border-white/10 shadow-xl shadow-green-500/10">
                <svg class="w-24 h-24 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
                        d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 002 2h2a2 2 0 002-2z">
                    </path>
                </svg>
            </div>
            <h1 class="text-3xl font-bold mb-4">AI 건강 분석</h1>
            <p class="text-gray-400">AI가 10가지 항목을 정밀하게 분석하여<br>숨겨진 질병을 찾아냅니다.</p>
        </div>

        <!-- Slide 3 -->
        <div id="slide-2"
            class="slide absolute inset-0 transition-opacity duration-500 opacity-0 pointer-events-none flex flex-col items-center text-center">
            <div
                class="w-48 h-48 bg-white/5 rounded-full flex items-center justify-center mb-8 border border-white/10 shadow-xl shadow-purple-500/10">
                <svg class="w-24 h-24 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
                        d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
            </div>
            <h1 class="text-3xl font-bold mb-4">체계적인 기록 관리</h1>
            <p class="text-gray-400">검사 결과를 차곡차곡 쌓아<br>반려견의 건강 변화를 한눈에 보세요.</p>
        </div>
    </div>

    <!-- Dots -->
    <div class="flex gap-2 mb-12">
        <div id="dot-0" class="dot w-2 h-2 rounded-full bg-white"></div>
        <div id="dot-1" class="dot w-2 h-2 rounded-full bg-white/30"></div>
        <div id="dot-2" class="dot w-2 h-2 rounded-full bg-white/30"></div>
    </div>

    <!-- Button -->
    <button id="next-btn" onclick="nextSlide()"
        class="w-full max-w-sm bg-primary text-white font-bold py-4 rounded-xl shadow-lg shadow-primary/30 transition-transform active:scale-95 cursor-pointer z-50">
        다음
    </button>
</div>

<script>
    // Global scope for onclick
    window.currentIndex = 0;

    window.nextSlide = function () {
        if (window.currentIndex < 2) {
            window.currentIndex++;
            updateSlide(window.currentIndex);
        } else {
            // Go to Registration
            window.location.href = "/new";
        }
    };

    function updateSlide(index) {
        const slides = [document.getElementById('slide-0'), document.getElementById('slide-1'), document.getElementById('slide-2')];
        const dots = [document.getElementById('dot-0'), document.getElementById('dot-1'), document.getElementById('dot-2')];
        const nextBtn = document.getElementById('next-btn');

        slides.forEach((slide, i) => {
            if (i === index) {
                slide.classList.remove('opacity-0', 'pointer-events-none');
                slide.classList.add('opacity-100');
            } else {
                slide.classList.remove('opacity-100');
                slide.classList.add('opacity-0', 'pointer-events-none');
            }
        });

        dots.forEach((dot, i) => {
            if (i === index) {
                dot.classList.remove('bg-white/30');
                dot.classList.add('bg-white');
            } else {
                dot.classList.remove('bg-white');
                dot.classList.add('bg-white/30');
            }
        });

        if (index === 2) {
            nextBtn.textContent = "시작하기";
        } else {
            nextBtn.textContent = "다음";
        }
    }
</script>